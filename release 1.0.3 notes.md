# 开放启动器 1.0.3

## 本次更新

- 全界面统一为精致中文像素风，BoutiqueBitmap9x9 同时覆盖中文正文、标题和核心数据，避免标题字体缺字后回退。
- 旧版保存的思源黑体设置会自动迁移到像素字体，1.0.1 和 1.0.2 用户升级后无需手动切换。
- 卡片、按钮、输入框、组件库、对话框、侧栏和首次启动页统一使用 6–16dp 的软圆角像素层级。
- 时间、天气温度和海拔等主要读数移动到组件视觉中心，速度与车况数字继续保持居中仪表布局。
- 首次启动页完整接入 OEM、赛博科技、极简玻璃三套主题和浅色模式，不再固定显示黑色背景。
- 清理仪表组件中的固定黑灰色，时钟、指南针、速度表、高度计、车况和侧栏完整跟随主题与自定义颜色动画。
- 指南针方向、行程状态和字体设置项进一步汉化，保留 CarPlay、Android Auto、AM/FM 和计量单位等官方名称。
- 天气刷新只在成功后进入 30 分钟周期；失败后 5 分钟可重试，车辆跨城时仍可立即取消旧请求并获取新地点天气。
- 天气缓存继续执行 12 小时到期清理和 50 公里跨城失效保护。
- 版本统一为 `1.0.3`（`versionCode=10003`）。

## 字体授权

字体随 APK 和源码分发，授权文件位于 `app/src/main/assets/licenses/`：

- BoutiqueBitmap9x9：SIL Open Font License 1.1
- FashionBitmap16：SIL Open Font License 1.1
- ChillBitmap：SIL Open Font License 1.1 及作者声明

FashionBitmap16 继续随源码保留，但不再用于中文界面标题，以避免字形缺失。

## 安装说明

Release APK 使用项目既有测试证书签名，适合个人车机安装和验证。覆盖升级 1.0.1 或 1.0.2 时必须继续使用相同签名证书。
